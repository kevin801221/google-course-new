# 複製成 travel_planner/.env（.env 已被 adk 產生的 .gitignore 排除，不要進版控）

# ── 路線 A：Developer API（AI Studio key，本 Lab 走這條）──
GOOGLE_GENAI_USE_ENTERPRISE=0
GOOGLE_API_KEY="AIza..."

# ── 路線 B：Enterprise / Vertex（M5 設好的專案，本 Lab 用不到）──
# GOOGLE_GENAI_USE_ENTERPRISE=True
# GOOGLE_CLOUD_PROJECT=agent-course-2026
# GOOGLE_CLOUD_LOCATION=us-central1
# （2025 舊教學寫 GOOGLE_GENAI_USE_VERTEXAI=TRUE，仍相容）

# Lab 6 的天氣 MCP server（http 模式）；沒設就用這個預設值
MCP_URL="http://localhost:8080/mcp"
