# 複製成 concierge/.env（.env 不要進版控）

# ── 路線 A：Developer API（AI Studio key，本 Lab 走這條）──
GOOGLE_GENAI_USE_ENTERPRISE=0
GOOGLE_API_KEY="AIza..."

# 服務 B 的位置。host 要跟名片上寫的一字不差：localhost ≠ 127.0.0.1
HOTEL_SERVICE_URL="http://localhost:8001"
